{

  "env": {

    // Add browser environment globals
    "browser": true,

    // Add mocha environment globals
    "mocha": true

  },
  "globals": {

    // Add he to environment globals but do not authorize to overwrite it
    "he": false,

    // Add angular to environment globals but do not authorize to overwrite it
    "angular": false,
    "inject": false,

    // Add chai to environment globals but do not authorize to overwrite it
    "chai": false,
    "assert": false,

    // Add protractor environment globals but do not authorize to overwrite it
    "element": true,
    "by": true,
    "protractor": true,
    "browser": true,

    // Add OpenVeo Portal environment globals but do not authorize to overwrite it
    "openVeoPortalSettings": true,
    "WowzaPlayer": true

  },
  "rules": {

    // Require JSDoc comment
    "require-jsdoc": 0

  }

}
